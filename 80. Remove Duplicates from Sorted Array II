class Solution {
    public int removeDuplicates(int[] nums) {
        if(nums.length==0){
            return 0;
        }
        if(nums.length==1){
            return 1;
        }
        int times=0;
        int start=1;
        for(int i=1;i<nums.length;i++){
            if(nums[i]!=nums[i-1]){
                nums[start]=nums[i];
                start++;
                times=0;
            }else{
                if(times==0){
                    nums[start]=nums[i];
                    start++;
                    times++;
                }else{
                    continue;
                }
            }
        }
        return start;
    }
}


之前那个题的改进版，重复出现一次也可以
