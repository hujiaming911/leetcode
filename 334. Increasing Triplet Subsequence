就是问在一个数组中有没有随着递增的顺序（不一定是挨着的），值也递增的三个数
那就把这个数组递增呗
第一个数找个变量记录下来
以后有比他大的就再找个变量记录下来
要是再有比第二个变量大的就返回true.走一圈没有就返回false
期间如果比这些变量小就用这些小的值替换变量就可以


class Solution {
    public boolean increasingTriplet(int[] nums) {
        int min=Integer.MAX_VALUE;
        int secmin=Integer.MAX_VALUE;
        for(int i=0;i<nums.length;i++){
            if(nums[i]<=min){
                min=nums[i];
            }else if(nums[i]<=secmin){
                secmin=nums[i];
            }else if(nums[i]>secmin){
                return true;
            }
        }
        return false;
    }
}
