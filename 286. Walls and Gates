给一个二维数组
由 -1 0 和 Inf 组成   -1代表墙，此路不通     0代表门     Inf代表一个一个点   Inf是小于Integer.MAX_VALUE的最大值
现在要把每个Inf改成到最近门的距离

DFS


public void wall(int[][] rooms){
   for(int i=0;i<rooms.length;i++){
       for(int j=0;j<rooms[0].length;j++){
           if(rooms[i][j]==0){
              dfs(rooms,i,j,0);
           }
       }
   }
}


public void dfs(int[][] rooms, int i, int j, int dis){
  if(i<0||i==rooms.length||j<0||j==rooms[0].length||rooms[i][j]==-1){
     return;
  }
  rooms[i][j]=dis;
  dfs(rooms,i+1,j,dis+1);
  dfs(rooms,i-1,j,dis+1);
  dfs(rooms,i,j+1,dis+1);
  dfs(rooms,i,j-1,dis+1);
}
