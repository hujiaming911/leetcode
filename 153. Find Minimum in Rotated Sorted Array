class Solution {
    public int findMin(int[] a) {
    int low = 0, mid = 0;
    int high = a.length - 1;
    while(low <= high) {
    	mid = low + ((high - low)/2);
    	if(a[mid] < a[high]) {
    		high = mid;
    	}else if(a[mid] > a[high]) {
    		low = mid+1;
    	}else {
    		return a[mid];
    	}
    }
    return 123;
}
}

中间的如果比右面的小，说明右面递增，则把范围缩至end=mid
中间的如果比右面大，则start=mid+1;
一直到中间的等于右面的就找到了最小的
